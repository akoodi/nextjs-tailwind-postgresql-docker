import prisma from "@/database/db-prisma";

export default async function QuoteData() {
	/*
	const posts = await prisma.quotes.findMany({
		select: {
			
		},
		orderBy: {
			
		},
	});
	
	return posts; 
	*/
}
